[project]
name = "jobs"
version = "0.1.0"
description = "Finland Job Market Visualizer — AI exposure analysis of Finnish occupations (StatFin PxWeb)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
]

[project.scripts]
finlandjobs = "main:main"
